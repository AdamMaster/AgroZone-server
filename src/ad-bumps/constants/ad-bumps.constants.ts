// Цена разового платного поднятия объявления, в копейках. Плейсхолдер —
// поменять на реальную цену одним значением здесь (сумма не берётся от
// клиента никогда, см. AdBumpsService.createCheckout — иначе можно было бы
// "заплатить" 1 копейку, поменяв тело запроса).
export const AD_BUMP_PRICE_KOPECKS = 9900
