// Цена платной услуги "Поднять объявление", в копейках. Сумма не берётся
// от клиента никогда (см. AdBumpsService.createCheckout — иначе можно
// было бы "заплатить" 1 копейку, поменяв тело запроса).
export const AD_BUMP_PRICE_KOPECKS = 14900

// На сколько дней активируется/продлевается услуга при успешной оплате.
// Всё это время объявление раз в сутки автоматически поднимается в топ —
// см. AdAutoBumpWorker. Повторная покупка ПРОДЛЕВАЕТ Ad.bumpServiceUntil
// (складывает эти дни поверх текущего значения, если оно ещё не истекло),
// а не перезаписывает — та же логика, что и в PremiumService.
export const BUMP_SERVICE_DURATION_DAYS = 7
